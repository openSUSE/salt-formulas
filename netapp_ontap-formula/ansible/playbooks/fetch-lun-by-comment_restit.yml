---
- hosts: localhost
  name: Get LUN
  connection: local
  gather_facts: False

  tasks:
    - name: Gather details
      block:
        - name: Query LUN
          import_tasks: '../tasks/get_lun_by_name_restit.yml'

