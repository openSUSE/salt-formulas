---
- hosts: localhost
  name: Create LUN
  connection: local
  gather_facts: False

  tasks:
    - name: Deploy
      block:
        - name: Map LUN
          import_tasks: '../tasks/unmap_lun_restit.yml'
